stages:
  arxiv:
    cmd: python arxiv.py --directories cifar mnist cifar100 --extensions pdf py yaml csv --title "Survival Analysis for Deep Convolutional Neural Networks"
    deps:
      - arxiv.py
      - cifar/
      - mnist/
      - cifar100/
      - mnist/
      - cifar/
    outs:
      - "Survival Analysis for Deep Convolutional Neural Networks.zip"
  # render dvc_plots:
  #   foreach:
  #     - mnist
  #     - cifar
  #     - cifar100
  #   do:
  #     cmd: dvc plots show
  #     plots:
  #       - ${item}/aft_comparison.csv:
  #           x : Distribution
  #           y : Concordance
  #           title : Concordance for each Distribution for the ${item} dataset